<UserControl x:Class="Quiz.Views.CreateQuizView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quiz.Views"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <TextBlock Text="{Binding WelcomeMessage}" FontSize="{Binding FontSize}"
                Margin="20" DockPanel.Dock="Top"/>
        <Slider Minimum="5" Maximum="50" Value="{Binding FontSize}" DockPanel.Dock="Bottom" />
    </DockPanel>
</UserControl>
